# 계산식 파서 구현

***

### *Divide & Conquer*

- 최상위 연산자 탐색 후 좌우의 식에 연산 적용 (recursion)
- 재귀적 호출 시 호출 순서와 작업 순서는 서로 반대 ( LIFO )
    - 식의 우측에서부터 최상위 연산자에 대한 재귀 호출을 통해 좌->우로
      <br> 진행되는 연산 순서 반영
    - 곱셈 연산자는 연산 우선 순위가 높으므로 먼저 수행되어야 함
      <br> 덧셈, 뺄셈 연산자가 존재하지 않을 때에만 연산자로 선택되도록 하여 나중에 호출되도록 구현

